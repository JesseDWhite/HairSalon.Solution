@{
  Layout = "_Layout";
}

@model HairSalon.Models.Stylist

<h2>Are you sure you want to delete this @Html.DisplayFor(model => model.Name)</h2>

@using (Html.BeginForm())
{
  <input type="submit" value="Delete" class="btn btn-danger mt-5 mt-5" />
}
@Html.ActionLink("Back to All Stylists", "Index")